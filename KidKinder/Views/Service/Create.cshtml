@model KidKinder.Models.Entities.Service
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="container mt-5">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <!-- Modern Form Card Start -->
            <div class="card shadow">
                <div class="card-header bg-success text-white">
                    <h3 class="card-title">Yeni Hizmet Kayıt Sayfası</h3>
                </div>
                <div class="card-body">
                    @using (Html.BeginForm("Created", "Service", FormMethod.Post, new { @class = "needs-validation", novalidate = "novalidate" }))
                    {
                        <div class="form-group mb-3">
                            @Html.HiddenFor(x => x.ServiceId)
                        </div>
                        <div class="form-group mb-3">
                            <label for="Title">Başlık:</label>
                            @Html.TextBoxFor(x => x.Title, new { @class = "form-control form-control-lg", placeholder = "Başlık giriniz", @required = "required" })
                            <div class="invalid-feedback">Lütfen bir başlık giriniz.</div>
                        </div>
                        <div class="form-group mb-3">
                            <label for="Description">Açıklama:</label>
                            @Html.TextBoxFor(x => x.Description, new { @class = "form-control form-control-lg", placeholder = "Açıklama giriniz", @required = "required" })
                            <div class="invalid-feedback">Lütfen bir açıklama giriniz.</div>
                        </div>
                        <div class="form-group mb-4">
                            <label for="IconUrl">İkon URL:</label>
                            @Html.TextBoxFor(x => x.IconUrl, new { @class = "form-control form-control-lg", placeholder = "İkon URL giriniz", @required = "required" })
                            <div class="invalid-feedback">Lütfen bir ikon URL'si giriniz.</div>
                        </div>
                        <button type="submit" class="btn btn-lg btn-success">KAYDET</button>
                    }
                </div>
            </div>
            <!-- Modern Form Card End -->
        </div>
    </div>
</div>

<script>
// Example starter JavaScript for disabling form submissions if there are invalid fields
(function() {
  'use strict';
  window.addEventListener('load', function() {
    // Fetch all the forms we want to apply custom Bootstrap validation styles to
    var forms = document.getElementsByClassName('needs-validation');
    // Loop over them and prevent submission
    var validation = Array.prototype.filter.call(forms, function(form) {
      form.addEventListener('submit', function(event) {
        if (form.checkValidity() === false) {
          event.preventDefault();
          event.stopPropagation();
        }
        form.classList.add('was-validated');
      }, false);
    });
  }, false);
})();
</script>
